{
  "name": "ases-blog-lambda",
  "version": "1.0.0",
  "description": "Lambda function for Ases Kahraba blog API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.450.0",
    "drizzle-orm": "^0.29.0",
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
